# Identity Software Integration Patterns

This document outlines the standard identity patterns used within the MoJ to ensure services can talk to each other securely while protecting user data.

## On behalf of (OBO) Pattern

### Use Case

OBO flows are required when an application needs to act as a middleman. It exchanges the user's initial login token for a new token intended for a different downstream service, all while maintaining the identity and permissions of the logged-in user.

Imagine giving a lawyer "Power of Attorney" to sign a house deed for you. The lawyer doesn't sign as themselves; they sign on your behalf using the specific authority you granted them.

### Practical example

#### The Issue

A team requests a new Application Registration. The application is built in AWS, public facing and the purpose of the application integrating is to

* Sign in a user (`User.Read.All` delegated)
* User will upload a document that will be saved to SharePoint (`SharePoint.ReadWrite.OwnedBy`)
* Application will call a centralised MOJ API Endpoint to store data such as UPN, Entra ID, DateTime, SharePoint ID, etc

If we created an application registration, we would have to grant both permissions on the application. The seperate centralised API Endpoint that the application calls will have to trust that the details sent are valid. This will require management of another custom API Auth method between the Application and the centralised API.

In this scenario, if a secret for this application is stolen, a bad actor could cause a large amount of damage by being able to upload files into SharePoint and trigger API calls to the centralised MOJ API Endpoint without any authentication checks.

#### OBO Pattern Fix

We can use OBO to fix this issue.

We Create 3 Application Registrations

* Service A: Public facing Web Application (Delegated `User.Read.All`)
* Service B: Non public facing Internal API (`SharePoint.ReadWrite.OwnedBy`)
* Service C: Centralised API Endpoint

C (Centralised API) trusts B (Internal API) to pass the user's identity securely. Service C validates the token to ensure the request is coming from a real user, preventing data spoofing.

In this way, even if a bad actor steals the Web Application's secrets, they cannot upload files. The Internal API requires a User Token (acting on behalf of a human). A stolen App Secret only proves the identity of the server, not the user, so the Internal API will reject the request.

``` mermaid
sequenceDiagram
    participant User
    participant Web as 1. Public Web App
    participant Internal as 2. Internal API (Orchestrator)
    participant SP as SharePoint
    participant Central as 3. Centralised MOJ API

    User->>Web: Upload Document
    Note over Web: App has NO permissions<br/>for SP or Central API.
    
    Web->>Internal: Send File + User Token A
    Note over Internal: Validates Token A.<br/>Exchanges for Token B (SP)<br/>Exchanges for Token C (Central)
    
    par Parallel/Sequential Actions
        Internal->>SP: Upload File (with Token B)
        SP-->>Internal: Return SharePoint ID
        
        Internal->>Central: Store Metadata (UPN, SP ID)<br/>(with Token C)
        Note right of Central: Extracts User identity<br/>from Token C.<br/>Cannot be spoofed.
    end
    
    Internal-->>Web: Success
    Web-->>User: Done
```

### OBO Technical Overview (Double Hop)

Flow: `User -> Web App -> Internal API --(call)--> External/Downstream API`

If an Internal API needs to call a second service (like a Shared Document API) as the user, it cannot reuse the token it was given. Tokens are "addressed" to a specific recipient (the Audience). The Internal API must trade the user's token for a new one addressed to the downstream service.

### Implementation Example

#### Setup your Applications in Terraform

You will be required to first setup your Application Registrations in Terraform. For this example we will create 3 applications.

**Source:** [Example Terraform Code.](https://github.com/ministryofjustice/staff-identity-idam-entra-infra/blob/main/terraform/envs/nleexternal/idam-testing/main.tf#L94)

> Important Note on Circular Dependencies: When creating these applications from scratch, Terraform may fail if you try to reference the `scope_id` of the Shared API inside the Internal API's configuration before the Shared API exists.

Step 1: Create all 3 app registrations in Terraform, but leave the `resource_access` blocks empty (or commented out). Apply this configuration.

Step 2: Once the Apps and their Scopes are created, update the `resource_access` block in the Internal API to reference the Shared API.

Step 3: Apply the Terraform again to create the link.

#### Example Node Implementation

##### Service A: The Frontend (obo-example-frontend)

The frontend's job is to request a token specifically for the "Middle Tier", Service B.

``` node
// Using @azure/msal-node
const msalConfig = {
    auth: {
        clientId: "FRONTEND_CLIENT_ID",
        authority: "https://login.microsoftonline.com/TENANT_ID",
        clientSecret: "FRONTEND_SECRET"
    }
};

const cca = new msal.ConfidentialClientApplication(msalConfig);

// The user logs in and we request access to the Internal API
const tokenRequest = {
    scopes: ["api://obo-example-internal-api/user_impersonation"],
    // code: ... (from the redirect)
};

const result = await cca.acquireTokenByCode(tokenRequest);
const accessTokenForInternalApi = result.accessToken;
```

##### Service B: The Internal API (obo-example-internal-api)

This is the "Middle Tier". This app has `resource_access` to the Shared API. It must perform the exchange.

``` node
const express = require('express');
const msal = require('@azure/msal-node');
const app = express();

const cca = new msal.ConfidentialClientApplication({
    auth: {
        clientId: "INTERNAL_API_CLIENT_ID",
        authority: "https://login.microsoftonline.com/TENANT_ID",
        clientSecret: "INTERNAL_API_SECRET"
    }
});

app.get('/process-documents', async (req, res) => {
    // 1. Extract the token sent from the Frontend
    const inboundToken = req.headers.authorization.split(' ')[1];

    // 2. Setup the OBO request for the Shared API
    const oboRequest = {
        oboAssertion: inboundToken,
        // Matches the 'value' in your Shared API terraform
        scopes: ["api://shared-documents/Documents.Read"], 
    };

    try {
        // 3. Exchange Frontend-Token for Shared-API-Token
        const response = await cca.acquireTokenOnBehalfOf(oboRequest);
        const sharedApiToken = response.accessToken;

        // 4. Call the Shared API
        // const data = await axios.get('...', { headers: { Authorization: `Bearer ${sharedApiToken}` } });
        
        res.send("Successfully chained identity to Shared API!");
    } catch (error) {
        res.status(500).send(error.message);
    }
});
```

##### Service C: The Shared API (obo-example-shared-api)

This service doesn't need to exchange tokens, it just needs to verify them.

``` node
const express = require('express');
const passport = require('passport');
const { BearerStrategy } = require('passport-azure-ad');

const options = {
    identityMetadata: "https://login.microsoftonline.com/TENANT_ID/v2.0/.well-known/openid-configuration",
    clientID: "SHARED_API_CLIENT_ID",
    audience: "api://shared-documents", // Matches your identifier_uris
    validateIssuer: true,
    passReqToCallback: false,
    loggingLevel: 'info'
};

const bearerStrategy = new BearerStrategy(options, (token, done) => {
    // Verify the scope exists in the token
    if (token.scp.includes("Documents.Read")) {
        return done(null, { name: token.name }, token);
    }
    return done(new Error("Missing Documents.Read scope"), null);
});

const app = express();
passport.use(bearerStrategy);

app.get('/docs', passport.authenticate('oauth-bearer', { session: false }), (req, res) => {
    res.json({ docs: ["Policy1.pdf", "Specs.docx"], user: req.user.name });
});
```

## Service to Service (S2S) Pattern

### Use Case

Use this for background tasks where no human user is present. This is for "Machine-to-Machine" communication, such as nightly backups, automated logging, or internal file synchronization.

#### Non-Tech Analogy

This is like a security guard having a master key to the building. They don't need a specific tenant to let them in; they have their own ID card that identifies them as an authorized employee.

#### Diagram

``` mermaid
sequenceDiagram
    participant Client as Consumer Service
    participant Entra as Entra ID
    participant SDS as SDS API (Resource)

    Note over Client, Entra: Step 1: Authentication
    Client->>Entra: Authenticate (via Managed Identity or Secret)<br/>Request token for SDS
    
    Note right of Entra: Validates Identity &<br/>Assigned App Roles
    
    Entra-->>Client: Issue Access Token<br/>(Contains Application Roles)

    Note over Client, SDS: Step 2: Access Resource
    Client->>SDS: Call API with Access Token
    
    Note left of SDS: Validates Token Signature<br/>Checks for 'Files.Write' Role
    
    SDS-->>Client: Return Success / Store File
```

### Terraform

> These Terraform examples are not to be used for production use or implemented into our repository. They are purely for psuedocode examples and the IDAM team will ensure a module is available to achieve these setups.

``` terraform
# =================================================
# 1. RANDOM UUIDS (For Stable Role IDs)
# =================================================
resource "random_uuid" "role_files_write" {}    
# App Role exposed by SDS API


# =================================================
# 2. RESOURCE API (The Service being called)
# =================================================
resource "azuread_application" "sds_api" {
  display_name     = "Secure Document Storage (SDS)"
  identifier_uris  = ["api://sds.internal.laa"]
  sign_in_audience = "AzureADMyOrg"

  # 2a. Define the App Role (Permission)
  app_role {
    allowed_member_types = ["Application"]                
    # 'Application' = S2S/Machine
    description          = "Allows background services to upload files."
    display_name         = "Files.Write"
    enabled              = true
    id                   = random_uuid.role_files_write.result
    value                = "Files.Write"
  }
}

# Service Principal for the API (Required for Role Assignment)
resource "azuread_service_principal" "sds_api_sp" {
  client_id = azuread_application.sds_api.client_id
}


# =================================================
# 3. CONSUMER SERVICE (The Background Worker)
# =================================================
resource "azuread_application" "client_service" {
  display_name = "Nightly Backup Service"

  # 3a. Request Access to the Resource API
  required_resource_access {
    resource_app_id = azuread_application.sds_api.client_id

    resource_access {
      id   = random_uuid.role_files_write.result      
      # The role ID from above
      type = "Role"                                   
      # 'Role' = S2S/Machine
    }
  }
}

# Service Principal for the Consumer (The Identity that runs the code)
resource "azuread_service_principal" "client_service_sp" {
  client_id = azuread_application.client_service.client_id
}

# 3b. Generate the Secret (The "Password" for the machine)
resource "azuread_application_password" "client_secret" {
  application_id = azuread_application.client_service.id
}


# =================================================
# 4. ROLE ASSIGNMENT (The "Admin Consent")
# =================================================

# Unlike User flows, S2S flows require explicit assignment (Admin Consent).
# This resource grants the 'Files.Write' role to the Backup Service.
resource "azuread_app_role_assignment" "grant_files_write" {
  app_role_id         = random_uuid.role_files_write.result           
  # The Permission
  principal_object_id = azuread_service_principal.client_service_sp.object_id 
  # The Consumer (Backup Service)
  resource_object_id  = azuread_service_principal.sds_api_sp.object_id    
  # The Resource (SDS API)
}
```

### Technical Requirements

* **Grant Type:** client_credentials
* **Entra Identity:** Managed Identity (strongly preferred for Azure resources to avoid secret management) or App Registration with a Client Secret.
* **Permissions:** Application Roles. These are assigned globally to the service identity rather than a specific user.

## Which should you use?

| If the action is... | Use this Flow | Permission Type |
| -- | -- | -- |
| A background cron job cleaning up old files. | S2S | Application Role |
| A system log upload from a worker node. | S2S | Application Role |
| A user viewing their own uploaded documents. | OBO | Delegated Scope |
| An admin service acting on a specific user's file. | OBO | Delegated Scope |

## Resources

* [Microsoft identity platform and OAuth 2.0 On-Behalf-Of flow](https://docs.azure.cn/en-us/entra/identity-platform/v2-oauth2-on-behalf-of-flow)
* [Using service-to-service (S2S) authentication](https://learn.microsoft.com/en-us/dynamics365/business-central/dev-itpro/administration/automation-apis-using-s2s-authentication)
* [EUCS IDAM Team Shared Inbox](mailto:idam@justice.gov.uk)

<script src="../../javascripts/mermaid.vendor"></script>
